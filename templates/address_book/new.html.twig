{% extends 'base.html.twig' %}

{% block title %}New AddressBook{% endblock %}

{% block body %}
<div class="card w-50 m-auto">
    <div class="card-body">
    <h1>Create new AddressBook</h1>

    {{ include('address_book/_form.html.twig') }}
    <hr>
    <a class="btn btn-dark" href="{{ path('address_book_index') }}">back to list</a>
    </div>
</div>
{% endblock %}
